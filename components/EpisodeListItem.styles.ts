import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  episodeCard: {
    backgroundColor: '#fff',
    borderRadius: 12,
    padding: 12,
    borderWidth: 1,
    borderColor: '#ddd',
  },
  episodeTitle: {
    fontWeight: '600',
  },
  episodeMeta: {
    marginTop: 4,
    color: 'gray',
  },
}); 